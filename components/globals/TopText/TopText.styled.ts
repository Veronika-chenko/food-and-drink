import styled from 'styled-components';

export const Text = styled.p`
  margin-bottom: 16px;

  font-weight: 500;
  font-size: 14px;
  line-height: 1.2;

  text-align: center;
  color: var(--accent);
`;
